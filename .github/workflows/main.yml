name: CI Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run tests (23f3004489@ds.study.iitm.ac.in)
        run: echo "Running tests  - 23f3004489@ds.study.iitm.ac.in"
      
